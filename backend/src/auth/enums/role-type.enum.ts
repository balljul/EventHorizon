/**
 * Enum defining the available roles in the application.
 * Used for role-based access control.
 * 
 * @enum RoleType
 * @author Philipp Borkovic
 */
export enum RoleType {
  USER = 'USER',
  ADMIN = 'ADMIN',
  ORGANIZER = 'ORGANIZER',
  ATTENDEE = 'ATTENDEE',
} 